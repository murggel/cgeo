<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/checkbox_matrix_fragment"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/cols_label"
        style="@style/text_label"
        android:textSize="@dimen/textSize_detailsSecondary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/matrix"
        app:layout_constraintLeft_toLeftOf="@id/matrix"
        app:layout_constraintRight_toRightOf="@id/matrix"
        tools:text="Terrain"
        />

    <TextView
        android:id="@+id/rows_label"
        style="@style/text_label"
        android:textSize="@dimen/textSize_detailsSecondary"
        android:rotation="270"
        app:layout_constraintTop_toTopOf="@id/matrix"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        tools:text="Difficulty"
        />

    <LinearLayout
        android:id="@+id/matrix"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cols_label"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="@dimen/textSize_detailsSecondary" />

</merge>
